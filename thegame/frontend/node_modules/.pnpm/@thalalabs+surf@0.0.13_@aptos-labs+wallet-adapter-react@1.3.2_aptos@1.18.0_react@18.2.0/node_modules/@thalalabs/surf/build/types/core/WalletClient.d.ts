import { useWallet } from '@aptos-labs/wallet-adapter-react';
import type { ABIRoot, ABIWalletClient, EntryOptions, EntryPayload, TransactionResponse } from '../types/index.js';
type Wallet = ReturnType<typeof useWallet>;
export declare class WalletClient {
    private wallet;
    private client;
    constructor({ wallet, nodeUrl }: {
        wallet: Wallet;
        nodeUrl: string;
    });
    submitTransaction(payload: EntryPayload, _?: EntryOptions | undefined): Promise<TransactionResponse>;
    useABI<T extends ABIRoot>(abi: T): ABIWalletClient<T>;
}
export {};
//# sourceMappingURL=WalletClient.d.ts.map